# Font Generator Free - 会员系统架构

## 系统架构图

```
┌─────────────────────────────────────────────────────────┐
│                    用户浏览器                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  index.html  │  │   auth.js    │  │membership.js │  │
│  │  (静态页面)   │  │ (Google登录) │  │  (会员功能)   │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
           │                    │                    │
           │                    │                    │
           ▼                    ▼                    ▼
┌─────────────────────────────────────────────────────────┐
│              Cloudflare Pages (静态托管)                  │
│              fontgeneratorfree.online                    │
└─────────────────────────────────────────────────────────┘
                                │
                                │ API 调用
                                ▼
┌─────────────────────────────────────────────────────────┐
│           Cloudflare Workers (API 后端)                  │
│         font-generator-api.workers.dev                   │
│  ┌──────────────────────────────────────────────────┐  │
│  │  /api/user/sync    - 用户同步                     │  │
│  │  /api/user/status  - 获取会员状态                 │  │
│  │  /api/history/save - 保存历史记录 (Pro)           │  │
│  │  /api/history/list - 获取历史记录 (Pro)           │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                                │
                                │ SQL 查询
                                ▼
┌─────────────────────────────────────────────────────────┐
│              Cloudflare D1 (SQLite 数据库)               │
│  ┌──────────────────────────────────────────────────┐  │
│  │  users      - 用户表                              │  │
│  │  history    - 历史记录表                          │  │
│  │  payments   - 支付记录表                          │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

## 功能模块

### 1. 免费功能（已完成）
- ✅ 30 种字体样式转换
- ✅ 实时预览
- ✅ 一键复制
- ✅ Google OAuth 登录

### 2. Pro 功能（待实现）
- 📝 历史记录云端保存
- 📦 批量文本转换
- 🖼️ 导出为图片/PDF
- 🎨 自定义字体组合

### 3. 支付集成（待实现）
- 💳 Stripe 订阅支付
- 💰 月付 $2.99 / 年付 $19.99 / 终身 $9.99

## 文件清单

| 文件 | 用途 |
|------|------|
| `schema.sql` | 数据库表结构 |
| `wrangler.toml` | Worker 配置 |
| `worker/index.js` | Worker 主入口 |
| `worker/handlers.js` | 用户认证处理 |
| `worker/history.js` | 历史记录处理 |
| `membership.js` | 前端会员模块 |
| `upgrade-modal.html` | 升级弹窗 UI |
| `DEPLOYMENT.md` | 部署指南 |

## 下一步

1. **立即可做**：部署 Worker API + D1 数据库
2. **1-2 周后**：集成 Stripe 支付
3. **1 个月后**：添加历史记录、批量转换等 Pro 功能

需要我帮你执行部署吗？
